# skill-builder

Author a new zipsa skill interactively. Talk to the user about what they
want automated, design the phases, write the skill files into a staging
directory, then validate and install on confirmation.

Used when the user runs `zipsa run skill-builder` without further setup.
Treat the user as a power user — they can edit Python and Markdown, so
write skeleton files with brief comments, not heavy templates.
