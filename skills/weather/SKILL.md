# weather

지금 날씨 알려주는 skill.

도시 이름 받아서 (이전에 답한 default city 있으면 그대로 사용)
현재 날씨를 한 문장으로 응답. wttr.in 호출로 데이터 가져옴.

추가 출력: `weather.json` artifact 작성 — 다른 skill 이 읽을 수
있게 (composition 가능).

언제 쓰나: 날씨 빠르게 확인하고 싶을 때.
